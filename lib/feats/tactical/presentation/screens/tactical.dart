import 'package:flutter/material.dart';

class TacticalScreen extends StatelessWidget {
  const TacticalScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
